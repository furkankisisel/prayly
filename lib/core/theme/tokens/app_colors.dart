import 'package:flutter/material.dart';

/// Design token: color palette (both light & dark usage)
class AppColors {
  // Core brand gold
  static const Color gold = Color(0xFFC79A32); // refined warm gold

  // Dark surface & light surface
  static const Color darkBackground = Color(0xFF07101A); // deep navy
  static const Color darkSurface = Color(0xFF0F1C28);
  static const Color lightBackground = Color(0xFFFAF7F0); // warm off-white
  static const Color lightSurface = Color(0xFFF2EBDC);

  static const Color darkTextPrimary = Color(0xFFF5F2E9);
  static const Color lightTextPrimary = Color(0xFF111822);

  static const Color accentMoon = gold; // could adjust later
}
